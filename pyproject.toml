[project]
name = "aifp-aos"
version = "0.1.0"
description = "AiFinPay Autonomous OS"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy",
    "alembic",
    "pydantic-settings",
    "litellm",
    "google-adk",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["apps*", "tests*"]
