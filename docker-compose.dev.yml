# Updated with Postgres 17, Redis 8, security hardening
services:
  postgres:
    image: postgres:17-alpine
  redis:
    image: redis:8-alpine
  api:
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]
# ... rest as before
